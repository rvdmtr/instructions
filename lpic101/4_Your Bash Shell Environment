=======COMMANDS==========

pwd - print working directory
which - посмотреть где бинарник среди каталогов определенных в переменной PATH
type -P ls - покажет путь до бинарника, аналог which ls
type - проверяет чем является аргумент - бинарник, алиас, ссылка, функция, файл
type type; type cd; type ln



env | grep HOME - env - отображает список переменных данного пользователя
env | grep SHELL - env - отображает список переменных данного пользователя
echo $PWD - отобразить значение переменной
echo $LOGNAME
echo "$PATH" - интерпритирует содержимое в двойных кавычках ("" weak quotes)
echo '$PATH' - не интерпритирует, печатает то, что заключено в кавычках (''strong quotes)


SET - displays shell settings or shell variables for this session

SET -x - включить дебаг режим текущей оболочки/shell
set +x - выключить дебаг режим

---
Создание функции
function yo() {echo yo} - создать функцию, которая выводит на экран yo
unset yo - удалить созданную функцию

#в BASHe
shopt - отображение текущих опций оболочки и их состояния(вкл или выкл)
shopt -s histredit - включить опцию histreedit(если файл истории переполнится, он не будет очищен, каждая вновь введенная команда, перезапишет первую в файле)

export $YO=yo - создать переменную в текущем пользовательском окружении, данная переменная сущетсвует только в текущем SHELL'e, если залогиниться в параллельной сессии, переменной не будет в ней
